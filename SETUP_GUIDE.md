# 핵심가치 선정 투표 — 설정 & 배포 가이드 (실시간 후보 제안 버전)

비개발자도 따라 할 수 있도록 작성했습니다. 순서대로만 진행하면 됩니다.
**전체 소요시간: 약 15분**

> 이 버전은 후보를 미리 입력하지 않습니다. 참여자들이 토론 중 **실시간으로 후보를 제안**하고,
> 진행자가 정리한 뒤 투표를 시작합니다.

---

## 📦 구성 파일

| 파일 | 역할 | 어디에 |
|------|------|--------|
| `index.html` | 화면 (디자인) | GitHub |
| `app.js` | 동작 로직 | GitHub |
| `google-apps-script.gs` | 데이터 저장·집계 서버 | Google Apps Script |

> `index.html` + `app.js` 두 개만 GitHub에 올립니다.
> `.gs` 파일은 **Google Apps Script**에 붙여넣습니다.

---

## 1단계 · Google Sheets + Apps Script 만들기 (약 7분)

1. [sheets.new](https://sheets.new) 접속 → 빈 스프레드시트 생성
   - **시트 탭(시트1)은 그대로 두세요.** 필요한 시트(Sessions, Suggestions, Votes)는 코드가 자동 생성합니다.
2. 상단 메뉴 **확장 프로그램 → Apps Script**
3. 기본 코드를 모두 지우고 `google-apps-script.gs` 내용을 **전부 붙여넣기**
4. 💾 저장 (Ctrl+S)
5. 오른쪽 위 **배포 → 새 배포**
   - 유형(톱니바퀴) → **웹 앱**
   - **액세스 권한: `모든 사용자`** ⬅️ 매우 중요
   - **배포** 클릭
6. 권한 승인: 본인 계정 선택 → "확인되지 않은 앱" → **고급 → (이동) → 허용**
7. 나오는 **웹 앱 URL 복사** (형태: `https://script.google.com/macros/s/AKfy.../exec`)

---

## 2단계 · app.js에 URL 연결 (약 1분)

`app.js` 맨 위쪽 한 줄만 교체:

```javascript
// 변경 전
const GAS_URL = "여기에_본인_APPS_SCRIPT_웹앱_URL_을_붙여넣으세요";
// 변경 후
const GAS_URL = "https://script.google.com/macros/s/AKfy.../exec";
```

따옴표 안쪽만 교체하세요.

---

## 3단계 · GitHub + Vercel 배포 (약 5분)

1. GitHub 새 저장소 생성 (예: `core-value-vote`)
2. **Add file → Upload files** 로 `index.html`, `app.js`(URL 교체본) 업로드 → Commit
3. [vercel.com](https://vercel.com) → **Add New → Project** → 저장소 Import → **Deploy**
4. 배포 주소 확인 (예: `core-value-vote.vercel.app`)

> 추천 커밋 메시지: `feat: 핵심가치 투표 - 참여자 실시간 후보 제안 흐름으로 개편`

---

## 4단계 · 실제 사용 흐름

### 진행자 (큰 화면 / 노트북)
1. 접속 → **진행자로 시작** → 활동 제목 입력 → **세션 만들기**
2. **STEP 1 (후보 제안)**: 화면의 4자리 코드·링크를 참여자에게 공유
   - 참여자가 제안하면 실시간으로 목록에 모임 (대소문자만 다른 영문은 자동 병합, 제안 횟수 ×N 표시)
   - 비슷한 후보 합치기: **🔗 후보 병합** → 합칠 후보를 2개 이상 탭 → **선택 병합** → 대표 이름 입력
     (예: "협업"과 "팀워크"를 "협업"으로 통합 — 제안 횟수와 제안자가 합산됨)
     병합된 후보의 "병합 해제"를 누르면 원래대로 되돌릴 수 있습니다.
   - 투표에서 뺄 후보는 **체크 해제** (오타·중복·부적절 후보 정리)
   - 필요하면 **+ 직접 추가**로 진행자가 후보를 직접 넣을 수도 있음
3. 정리가 끝나면 **투표 시작 (후보 확정)** → 후보 제안 마감, 참여자 화면이 자동으로 투표로 전환
4. **STEP 2 (투표 진행)**: 투표 완료 인원 실시간 확인
5. **투표 마감 · 결과 집계** → **STEP 3**: 상위 3개 자동 산출, 동점 시 안내, 이미지 저장

### 참여자 (각자 휴대폰)
1. 진행자가 준 **링크 클릭** (또는 코드 입력) → 이름·팀 입력 → 참여
2. **후보 제안**: 떠오르는 핵심가치를 입력하고 제안 (여러 개 가능)
3. 진행자가 투표를 시작하면 **화면이 자동으로 투표로 전환** (새로고침 불필요)
4. 후보별 1~5점 투표 → 제출

---

## 🔧 자주 묻는 상황

**Q. 참여자 화면이 투표로 안 바뀌어요**
→ 진행자가 **투표 시작**을 눌렀는지 확인하세요. 약 4초 내 자동 전환됩니다.

**Q. 비슷한 후보가 따로따로 떠요 (예: "협업"과 "팀워크", "도전"과 "도 전")**
→ **🔗 후보 병합** 버튼을 누르고 합칠 후보들을 탭한 뒤 **선택 병합**을 누르세요.
  대표 이름을 정하면 하나로 합쳐지고 제안 횟수·제안자가 합산됩니다.
  (대소문자만 다른 영문은 자동 병합됩니다)

**Q. 참여 현황·결과가 안 나와요**
→ 1단계에서 **"모든 사용자"** 권한으로 배포했는지 확인하세요.
  권한을 바꿨다면 **배포 관리 → 편집 → 버전: 새 버전**으로 재배포해야 반영됩니다.

**Q. 같은 코드로 다시 하면 이전 제안이 남아있나요?**
→ 진행자가 **새 세션 시작**을 누르면 새 코드가 생성됩니다. 같은 코드로 createSession을
  다시 호출하면 그 코드의 이전 제안·투표 데이터는 자동 정리됩니다.

---

## 📊 Google Sheets 데이터 구조 (분석용 원본)

**Sessions** — code / title / phase / candidates(확정후보 JSON) / createdAt
**Suggestions** — code / name / team / candidate / suggestedAt
**Votes** — code / name / team / scores(JSON) / submittedAt

- `Suggestions`로 "어떤 후보가 얼마나 많이 제안됐는지" 분석 가능
- `Votes`의 `scores`는 `[{"name":"도전","score":5}, ...]` 형태로 후속 분석에 바로 활용
- 다운로드: 파일 → 다운로드 → Excel/CSV
